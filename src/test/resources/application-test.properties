# Test configuration for Campaign Analytics
spring.application.name=CampaignAnalytics-Test

# Test data paths (will be overridden by @TestPropertySource)
app.csv.input-path=test-input.csv
app.csv.output-dir=test-output

# Logging configuration for tests
logging.level.com.example.interview=DEBUG
logging.level.org.duckdb=WARN
